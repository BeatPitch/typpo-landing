# dev-only: masters, sources, mocks, backups — not for deploy
_dev/
.DS_Store

# dev convenience symlink + superseded page
hero-c.html
typpo-landing.standalone.html
.claude/

# vercel CLI link metadata
.vercel/
.vercel
